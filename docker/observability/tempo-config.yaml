# Tempo Configuration
# Distributed tracing for AI agent workflows
# Documentation: https://grafana.com/docs/tempo/latest/configuration/

server:
  http_listen_port: 3200
  grpc_listen_port: 9095
  log_level: info

# NOTE: OTLP receivers are handled by Alloy collector, not Tempo directly
# Tempo receives traces via remote_write from Alloy
distributor:
  receivers: {}

ingester:
  max_block_duration: 5m

compactor:
  compaction:
    block_retention: 168h  # 7 days

metrics_generator:
  registry:
    external_labels:
      source: tempo
      cluster: guilde-lite
  storage:
    path: /var/tempo/generator/wal
    remote_write:
      - url: http://mimir:9009/api/v1/push
        send_exemplars: true
  traces_storage:
    path: /var/tempo/generator/traces
  processor:
    service_graphs:
      dimensions:
        - agent.type
        - tool.name
        - skill.name
    span_metrics:
      dimensions:
        - agent.type
        - tool.name
        - skill.name
        - session.id

storage:
  trace:
    backend: local
    wal:
      path: /var/tempo/wal
    local:
      path: /var/tempo/blocks

querier:
  frontend_worker:
    frontend_address: localhost:9095

overrides:
  defaults:
    metrics_generator:
      processors:
        - service-graphs
        - span-metrics

# Span attributes for AI agent tracing
# Expected attributes:
# - session.id: Claude Code session identifier
# - agent.type: Agent/subagent type (haiku, sonnet, opus)
# - tool.name: Tool being used (Read, Write, Bash, etc.)
# - skill.name: Skill/command being executed
# - context.tokens: Token count for context
# - context.compaction_count: Number of compactions
