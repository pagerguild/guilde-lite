# Stage 6: Cloud & AWS Tools
# AWS CLI and modern authentication
# Install: brew bundle --file=brew/06-cloud.Brewfile

tap "common-fate/granted"   # Modern AWS SSO

brew "awscli"
brew "granted"              # Fast AWS role switching
brew "session-manager-plugin"  # SSH via SSM (no keys needed)
